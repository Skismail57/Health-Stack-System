# Django credentials
DEBUG=
SECRET_KEY=""
ALLOWED_HOSTS="127.0.0.1,localhost"

# Mailtrap credentials 
SMTP_HOST=''
SMTP_PORT=
SMTP_USER=''
SMTP_PASSWORD=''

# Payment Gateway (Offline) - SSL COMMERZ credentials 
STORE_ID=''
STORE_PASSWORD=''
STORE_NAME=''

# Database configuration (set DB_ENGINE to 'postgres' to use Postgres)
DB_ENGINE='sqlite'
DB_NAME='healthstack'
DB_USER='healthstack'
DB_PASSWORD='healthstack'
DB_HOST='db'
DB_PORT='5432'

# Security (applied when DEBUG=False)
SECURE_SSL_REDIRECT=1
SESSION_COOKIE_SECURE=1
CSRF_COOKIE_SECURE=1
SECURE_HSTS_SECONDS=3600

# Email backend (override in production)
EMAIL_BACKEND='django.core.mail.backends.console.EmailBackend'

# Sentry (optional)
SENTRY_DSN=''
SENTRY_TRACES_SAMPLE_RATE=0.1

# Redis & Celery (for caching, sessions, and background tasks)
REDIS_URL='redis://localhost:6379/1'
CELERY_BROKER_URL='redis://localhost:6379/0'
CELERY_RESULT_BACKEND='redis://localhost:6379/0'

# CORS (Cross-Origin Resource Sharing) - Production only
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS='https://yourdomain.com,https://www.yourdomain.com'